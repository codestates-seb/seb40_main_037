import styled from 'styled-components';
import { container } from '../../styles';

export const Container = styled.main`
  position: relative;
  ${container};

  .Mypage {
    position: relative;
    max-width: none;
  }
`;
